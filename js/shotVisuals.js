// Compatibilidade: mantém o modal original da aplicação intacto.
// A camada visual nativa foi desativada temporariamente para não remover
// elementos do #shotModal utilizado pela lógica principal.

export function installShotCourt() {}
export function installShotGoal() {}
export function initShotVisuals() {
  // Não modificar o DOM do modal legado.
}
